// backend/config/aiConfig.js
export const AIConfig = {
  // Ограничения для разных типов запросов (в символах, приблизительные для оценки)
  MAX_PROMPT_LENGTH: 100000,      // Максимальная длина промпта
  MAX_TEXT_LENGTH: 100000,        // Максимальная длина текста документа
  MAX_COMBINED_TEXT_LENGTH: 100000, // Максимальная длина объединенного текста (для анализа документа + вложения)
  MAX_ANALYSIS_TEXT_LENGTH: 100000, // Максимальная длина текста для анализа
  MAX_CHRONICLE_TEXT_LENGTH: 100000, // Максимальная длина текста для летописи
  
  // Температура модели для разных задач (0.0-1.0, где 0.0 - детерминированный, 1.0 - креативный)
  TEMPERATURE: {
    DEFAULT: 0.5,                 // Температура по умолчанию
    ANALYSIS: 0.4,                // Температура для анализа юридических текстов
    CHRONICLE: 0.6,               // Температура для генерации летописи
    KEY_SENTENCES: 0.1,           // Температура для извлечения ключевых предложений
    COMPLAINT: 0.5,               // Температура для генерации жалоб
  },
  
  // Примечание: эти ограничения являются приблизительными и работают как предварительная фильтрация
  // Реальные ограничения модели измеряются в токенах, а не в символах
};