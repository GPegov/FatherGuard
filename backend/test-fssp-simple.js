// Простой тест для проверки работы парсера\nimport FSSPParser from './services/fsspParser.js';\n\nasync function testParser() {\n  console.log('Запуск теста парсера ФССП...');\n  \n  const parser = new FSSPParser();\n  \n  try {\n    // Тест получения тестовых данных\n    console.log('\\n1. Тест получения тестовых данных:');\n    const testData = parser.getTestRegionData({name: 'Тестовый регион'});\n    console.log('Тестовые данные созданы успешно');\n    console.log('Пример:', JSON.stringify(testData, null, 2).substring(0, 200) + '...');\n    \n    // Тест сохранения данных\n    console.log('\\n2. Тест сохранения данных:');\n    await parser.saveToFile([testData]);\n    console.log('Данные успешно сохранены');\n    \n    // Тест загрузки данных\n    console.log('\\n3. Тест загрузки данных:');\n    const loadedData = await parser.getData();\n    if (loadedData) {\n      console.log('Данные успешно загружены');\n      console.log(`Дата обновления: ${loadedData.timestamp}`);\n      console.log(`Регионов: ${loadedData.regions.length}`);\n    } else {\n      console.log('Нет сохраненных данных');\n    }\n    \n    console.log('\\n✅ Все тесты пройдены успешно!');\n    \n  } catch (error) {\n    console.error('❌ Ошибка при тестировании парсера:', error);\n  }\n}\n\n// Запуск теста только если файл запущен напрямую\nif (import.meta.url === `file://${process.argv[1]}`) {\n  testParser();\n}\n\nexport default testParser;