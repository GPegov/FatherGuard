// Тестовый скрипт для проверки новой системы классификации отделений по городам
import FSSPParser from './services/parsing/fsspParser.js';\n\nasync function testCityClassification() {\n  console.log('Тестирование новой системы классификации отделений по городам');\n  \n  // Тестируем Свердловскую область (регион по умолчанию)\n  console.log('\\n=== Тест 1: Свердловская область ===');\n  const parser66 = new FSSPParser(\"Свердловская область\");\n  console.log(`Регион: ${parser66.regionName}, Код: ${parser66.regionCode}`);\n  \n  // Проверяем загрузку известных городов\n  const knownCities66 = parser66.loadKnownCitiesForRegion();\n  console.log(`Загружено ${knownCities66.cities.length} известных городов`);\n  console.log(`Примеры городов: ${knownCities66.cities.slice(0, 5).join(', ')}`);\n  \n  // Тестируем извлечение города из адреса\n  const testAddress1 = \"620062 г. Екатеринбург, ул. Чебышёва, д. 4\";\n  const city1 = parser66.extractCityFromAddress(testAddress1);\n  console.log(`Адрес: ${testAddress1}`);\n  console.log(`Извлеченный город: ${city1}`);\n  \n  const testAddress2 = \"622051 г. Нижний Тагил, ул. Правды, д. 13\";\n  const city2 = parser66.extractCityFromAddress(testAddress2);\n  console.log(`Адрес: ${testAddress2}`);\n  console.log(`Извлеченный город: ${city2}`);\n  \n  // Тестируем Московскую область\n  console.log('\\n=== Тест 2: Московская область ===');\n  const parser50 = new FSSPParser(\"Московская область\");\n  console.log(`Регион: ${parser50.regionName}, Код: ${parser50.regionCode}`);\n  \n  // Проверяем загрузку известных городов\n  const knownCities50 = parser50.loadKnownCitiesForRegion();\n  console.log(`Загружено ${knownCities50.cities.length} известных городов`);\n  console.log(`Примеры городов: ${knownCities50.cities.slice(0, 5).join(', ')}`);\n  \n  // Тестируем извлечение города из адреса\n  const testAddress3 = \"141000 Московская обл., г. Мытищи, ул. Колонцова, д. 1\";\n  const city3 = parser50.extractCityFromAddress(testAddress3);\n  console.log(`Адрес: ${testAddress3}`);\n  console.log(`Извлеченный город: ${city3}`);\n  \n  // Тестируем Челябинскую область\n  console.log('\\n=== Тест 3: Челябинская область ===');\n  const parser74 = new FSSPParser(\"Челябинская область\");\n  console.log(`Регион: ${parser74.regionName}, Код: ${parser74.regionCode}`);\n  \n  // Проверяем загрузку известных городов\n  const knownCities74 = parser74.loadKnownCitiesForRegion();\n  console.log(`Загружено ${knownCities74.cities.length} известных городов`);\n  console.log(`Примеры городов: ${knownCities74.cities.slice(0, 5).join(', ')}`);\n  \n  // Тестируем извлечение города из адреса\n  const testAddress4 = \"454000 Челябинская обл., г. Челябинск, ул. Ленина, д. 1\";\n  const city4 = parser74.extractCityFromAddress(testAddress4);\n  console.log(`Адрес: ${testAddress4}`);\n  console.log(`Извлеченный город: ${city4}`);\n  \n  console.log('\\n=== Тестирование завершено ===');\n}\n\n// Запускаем тест\ntestCityClassification().catch(console.error);